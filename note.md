# Weiqing's note for setting up the environment 

1. Install micromamba
2. `micromamba create sflim python=3.12`
3. `micromamba activate sflim`
4. `micromamba install -c conda-forge cupy`
5. `pip install PyQt6`
6. `micromamba install -c conda-forge matplotlib scipy scikit-learn tqdm`